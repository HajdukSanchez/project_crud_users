package utils

// List of endpoints
const (
	Status  string = "/status"    // Page with server status
	NewUser string = "/user"      // C (Create) functions for user
	RUDUser string = "/user/{id}" // RUD (Read, Update, Delete) functions for user
)
